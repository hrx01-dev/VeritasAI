
  # Dark-Themed Dashboard UI (Community)

  This is a code bundle for Dark-Themed Dashboard UI (Community). The original project is available at https://www.figma.com/design/ktnGqwnUXTCnqn43erolCh/Dark-Themed-Dashboard-UI--Community-.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  